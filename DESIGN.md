# Design Specification: Frontend Engineer Portfolio

> 技術的な表現を入口に、実績、担当範囲、判断、成果を静かに読み進められるポートフォリオ。

## Status

- Scope: ポートフォリオサイト全体
- Direction: Computational Archive
- Maturity: Implementation-ready
- Last reviewed: 2026-08-05
- Baseline: Astro 7.0.7、TypeScript 6.0.3、SCSS、Three.js 0.185.1、Swiper 14.0.6
- Blocking open decisions: なし

この文書は現在有効な仕様だけを記載する。比較案、廃止した仕様、承認経緯、検証履歴は残さない。

## Product

### 目的

採用担当者、エンジニア、デザイナー、協業相手に、次の内容を短時間で伝える。

1. どのようなフロントエンド開発を経験してきたか
2. どのような制約や課題を扱ったか
3. 自分が何を判断し、担当したか
4. 何を実現したか
5. 詳細な事例や連絡先をどこで確認できるか

### 基本方針

- 表現で関心を引き、実績と判断で信頼を得る。
- 技術表現は精密かつ抑制的にし、常にコンテンツを優先する。
- 作品はカード一覧ではなく、資料性のあるケーススタディとして見せる。
- HTMLを情報の正本とし、WebGLやアニメーションは段階的な拡張として扱う。
- 強調は巨大な文字ではなく、余白、整列、境界線、コントラストで作る。

## Information Architecture

### Routes

- `/`
  - Hero
  - Profile
  - Career
  - Works
  - Contact
- `/works/[slug]/`
  - プロジェクト概要
  - メイン画像と画面ギャラリー
  - 技術構成
  - 7章のケーススタディ
  - 次の作品へのリンク
- `/404.html`
  - エラー説明
  - トップページへのリンク

`/works/` の一覧ページは作らない。トップページのWorksから各詳細ページへ直接遷移する。

### Navigation

- WideではロゴとProfile、Career、Works、Contactを常時表示する。
- Narrowではロゴとアイコンのみのメニューボタンを表示する。
- Narrowのメニューはネイティブの`dialog`を使用し、画面全体を覆う。
- 詳細ページのナビゲーションはトップページの各セクションへ戻る。
- スキップリンクを最初のフォーカス可能要素として配置する。

## Art Direction

### Computational Archive

全体は明るい技術資料のような佇まいとする。

- Heroの中心に、丸みのある多面体と3本の軌道を配置する。
- コンテンツ部分は、細い境界線、矩形、余白、番号、記録用紙のような構成を使う。
- Profile、Career、Works、Contactで共通の視覚言語を維持する。
- 作品画像を大きく扱い、隣接する情報は簡潔な記録として整理する。
- 装飾は技術的な意味を持つ形状に限定し、画面全体へ広げない。

### Prohibited

- 多色グラデーションやオーロラ背景
- 巨大なディスプレイ文字
- Glassmorphism、強いぼかし、発光表現
- 角丸カードを並べた一般的なポートフォリオ構成
- 技術ロゴの一覧
- カスタムカーソル、スクロールジャック、音
- 重要な情報をCanvasやホバー内だけに配置すること
- スクロールに連動した登場、変形、パララックス、セクション遷移

## Visual Foundation

### Color

ライトテーマのみとし、`color-scheme: light`を使用する。

| Token            | Value     | Use                            |
| ---------------- | --------- | ------------------------------ |
| `--color-paper`  | `#f7f8f5` | ページ背景                     |
| `--color-sheet`  | `#fcfdf9` | 情報面、Contact                |
| `--color-ink`    | `#151817` | 本文、主要な境界               |
| `--color-muted`  | `#626a67` | 補足文、メタデータ             |
| `--color-line`   | `#cbd0cd` | 区切り線                       |
| `--color-field`  | `#dfe4e1` | 画像の背景、代替表示           |
| `--color-accent` | `#315f68` | フォーカス、選択、限定的な強調 |

- `--color-line`を本文や重要な状態表示に使用しない。
- 装飾目的のグラデーションを使用しない。
- 新しい色は既存トークンで表現できない場合のみ追加する。

### Typography

- 書体は`Noto Sans JP`を使用し、ウェイトは`400..600`とする。
- フォールバックは`sans-serif`とする。
- Serif、明朝体は使用しない。
- 本文は読みやすい行長とし、長文には`text-wrap: pretty`を使用できる。
- 短い見出しには`text-wrap: balance`を使用できる。
- 見出しはビューポートの大部分を占有させない。
- 文字サイズはPCとNarrowでそれぞれ固定値を定義し、`rem()`を使用する。

### Layout

- コンテンツ幅は最大`1344px`相当とする。
- Header、Hero、Footerは最大`1920px`相当の共通シェルを使用する。
- レイアウトの切り替えは`768px`相当を基準とし、`mq(narrow)`と`mq(wide)`を使用する。
- 主要な面と画像は角丸を付けない。
- 境界は1px相当の細い実線を基本とする。
- 影は原則使用せず、必要な場合も低い不透明度に留める。

### Media and Icons

| Asset             | Ratio or size | Treatment                       |
| ----------------- | ------------- | ------------------------------- |
| Header logo       | `959 / 189`   | `/images/logo.svg`              |
| Favicon           | `32 × 32`     | `/images/favicon.png`           |
| Open Graph image  | `1200 × 630`  | `/images/og.png`                |
| Profile portrait  | `4 / 5`       | 直接トリミング、Corner Register |
| Work cover        | `16 / 10`     | 固定の横長フレーム              |
| Work detail stage | `16 / 10`     | メイン画像と画面ギャラリー      |

- 画像には`width`と`height`を指定し、レイアウトシフトを防ぐ。
- 汎用アイコンは`@lucide/astro`から名前付きで直接読み込む。
- Lucideアイコンは`currentColor`と既定のストロークを使用する。
- テキストを補助するアイコンは`aria-hidden="true"`とする。
- ブランド、Hero、メニューアイコン、シェーダー表現はLucideの対象外とする。

## Responsive Behavior

### Wide

- Heroは最低`100dvh`を確保する。
- Profileは画像と本文の2カラムとする。
- Careerは在籍期間を示す装飾スケールと経歴一覧を表示する。
- WorkFeatureは画像を左、情報を右に配置する。
- Work詳細は目次と技術構成を左、本文を右に配置する。
- Contactは複数カラムのOpen Letter構成とする。

### Narrow

- Headerはロゴとアイコンのみのメニューボタンに切り替える。
- HeroはブラウザUIの伸縮で高さが変動しないよう、最低`100svh`を確保する。
- すべての主要コンテンツを1カラムにする。
- Profile画像は`4 / 5`を維持する。
- Careerの装飾スケールを非表示にし、経歴を縦に並べる。
- WorkFeatureは画像を情報より先に表示する。
- Work詳細の目次は本文より前の通常フローへ移す。
- Workのシェーダーを停止し、静止画像を表示する。
- Contactのリンクは利用可能な横幅を使う。

200%の文字拡大でも、コンテンツの欠落、重なり、ページ全体の横スクロールを発生させない。

## Components

### SiteHeader

- Wideでは主要ナビゲーションを常時表示する。
- Narrowではネイティブ`dialog`を使った全画面メニューを表示する。
- メニュー面、ロゴ、閉じるボタンは即時表示し、通常ヘッダーとの重なりを見せない。
- メニュー項目は短いフェードと上方向への移動、フッターは遅延フェードで表示する。
- メニュー表示中は現在のスクロール位置を保持して背景を固定する。
- 閉じる操作、リンク選択、Escape、Wideへの切り替えでメニューを閉じる。
- リンクとボタンには明確な`:focus-visible`を表示する。

### HeroScene

- 読める情報は通常のHTMLとして配置し、Canvasは`aria-hidden="true"`とする。
- 中心に丸みのある多面体と、細い3本の軌道、移動するマーカーを表示する。
- ポインター操作には反応させず、自律的にゆっくり動かす。
- Narrowでは同じ構成を低負荷設定で表示する。
- Narrowでは幅が変わらない高さだけの変化でCanvas描画バッファを再生成しない。
- Reduced motion、WebGL2非対応、初期化失敗、context lossでは静止SVGへ切り替える。
- 画面外または非表示タブでは描画ループを停止する。

### SectionHeading

- Profile、Career、Works、Contactで同じ構成を使用する。
- 英語の見出し、短いアクセント線、装飾的な2桁番号を並べる。
- 番号は`aria-hidden="true"`とする。
- Catch copyや重複した見出しを追加しない。

### Profile

- `4 / 5`のポートレートに4つのコーナーマークと6つの目盛りを付ける。
- 装飾は画像サイズを変化させず、支援技術から隠す。
- Strength 01から03を同じ順序で縦に配置する。
- スキルは本文の文脈内で説明し、ロゴ一覧に分離しない。

### Career

- 経歴は新しい順に表示する。
- Wideでは在籍期間を3本の帯で表現する。
- Narrowでは装飾帯を非表示にし、期間、役割、概要の順に表示する。
- 装飾帯は`aria-hidden="true"`とし、経歴一覧を情報の正本とする。

### WorkFeature

- `16 / 10`の画像を左、Case Dossierを右に配置する。
- Narrowでは画像を先にした1カラムへ切り替える。
- 画像に4つのCorner Registerを付ける。
- 課題、役割、担当範囲、実現したことを常に読める状態で表示する。
- 画像、作品名、`詳細を見る`を同じ詳細ページへリンクする。
- 機密案件では組織名、サイト名、URL、内部構成、未確認の数値を公開しない。
- シェーダーが動作しなくても内容とナビゲーションを利用できるようにする。

### Work Interaction

- Fine pointerかつWideの場合だけ有効にする。
- ポインター付近の三角形だけを少量移動、回転、傾斜させて元へ戻す。
- 画像全体を歪めず、画像の認識性を維持する。
- 水面、波紋、円形軌道、全画面グリッド、欠落する断片を使用しない。
- Touch、Narrow、Reduced motion、初期化失敗では静止画像を表示する。

### Work Detail

- 見出し、概要、時期、規模、体制、役割、担当範囲を先に表示する。
- URLがある作品はH1を`text-link`でリンク化し、別ドメインの場合だけ新しいタブで開く。
- メイン画像と補足画像を1つのSwiperギャラリーにまとめる。
- ギャラリーはサムネイル、前後ボタン、キーボード操作に対応する。
- 自動再生、ループ、ズーム、装飾的なスライド効果を使用しない。
- 本文は目次、技術構成、7章のケーススタディで構成する。
- 章は背景・課題、担当範囲、技術判断、主な実装、品質、実現した状態、振り返りの順とする。
- 箇条書きは番号、上罫線、行区切りを持つ1列のEvidence Ledgerとする。
- 最下部に次の作品へのリンクを表示する。
- 次の作品リンクは20pxから30px相当とし、Lucideの`ArrowRight`を使用する。
- HoverとFocusでアクセントカラーへ変化し、アイコンを右へ移動する。
- Reduced motionではアイコン移動を停止する。

### Contact

- SectionHeadingに続けて、Open Letter形式の本文と連絡先を表示する。
- EmailとGitHubへの直接リンクを使用し、フォームは設置しない。
- LinkedInは表示しない。
- HoverとFocusは色または下線で表現し、レイアウトを動かさない。

## Motion

- 自律的な動きはHeroとScroll cueだけに使用する。
- 直接操作による動きはリンク、メニュー、カルーセル、Work画像に限定する。
- スクロール位置を視覚変化の入力にしない。
- 通常のリンク操作は`--motion-fast`を使用する。
- 全画面メニューの開閉と内容の追従には`--motion-fast`を使用する。
- すべての動きにReduced motionの状態を用意する。
- 非表示または操作終了後に永続的な描画ループを残さない。

## Accessibility

目標はWCAG 2.2 Level AAとする。

- ページごとに1つの`h1`を使用し、見出し階層を維持する。
- ランドマーク、リスト、定義リスト、ネイティブリンク、ネイティブボタンを優先する。
- すべての操作要素をキーボードで利用可能にする。
- `:focus-visible`へアクセント色のアウトラインとオフセットを指定する。
- 色、位置、動き、シェーダーだけで意味を伝えない。
- Canvasと装飾SVGは支援技術から隠し、同等の情報をHTMLで提供する。
- Narrowまたはcoarse pointerでは44px相当の操作領域を目安とする。
- 200%の文字拡大、Reduced motion、キーボード操作で内容を失わない。

## Content

- 日本語を基本とし、固有名詞、技術名、短いラベルだけ英語を使用する。
- 文章は具体的かつ事実ベースにし、根拠のない自己評価を避ける。
- 作品では課題、役割、担当範囲、実装、品質、実現した状態を明示する。
- 担当範囲とチーム全体の成果を混同しない。
- 未確認の削減率、成果指標、人数、規模を追加しない。
- 機密案件は一般化し、組織名、サイト名、URL、内部構成を公開しない。
- 公開前に雇用者名、案件説明、画像、外部リンクを確認する。

## Implementation Contract

### Runtime

- Astroですべての読める内容を静的生成する。
- 公開環境はS3とCloudFrontを前提とし、サーバー処理を使用しない。
- Three.jsはHeroでのみ動的に読み込む。
- Workシェーダーは対象画像が利用可能な場合だけ初期化する。
- WebGLの失敗は静的なHTMLと画像へFail openする。
- SwiperはWork詳細のギャラリーだけで使用する。
- Google Analyticsはgtagを非同期かつ低優先度で読み込む。

### Styling

- 色、余白、モーションは既存トークンを使用する。
- コンポーネントとページ固有のスタイルはBEMクラスだけで指定する。
- px相当の長さは`rem()`を使用し、整数のpx相当値を渡す。
- PCとNarrowで異なる長さは`mq()`で固定値を切り替える。
- レスポンシブ条件は`mq()`を使用する。
- 状態、疑似要素、親Modifierとの関係は元のBEMセレクター内へネストする。
- 詳細なSCSS規約は`AGENTS.md`を正本とする。

### Verification

- 通常の変更確認は`pnpm check`を使用する。
- UI変更は必要に応じてWideとNarrowをブラウザで確認する。
- 確認項目は横スクロール、フォーカス、Reduced motion、画像比率、静的フォールバックを含む。
- この静的サイトでは、ユーザーから明示されない限り自動テストを追加しない。

## Open Items

- 本番URLをAstroの`site`へ設定し、canonicalとOG URLを絶対URLにする。
- 残りの作品画像を最終素材へ置き換える。
- 雇用者名と案件説明の公開可否を最終確認する。
- WebGLの負荷を実機のiOSとAndroidで確認する。
- Safari、Firefox、200%文字拡大、支援技術で最終確認する。

## Maintenance

- `DESIGN.md`には常に現在有効な仕様だけを記載する。
- 仕様変更時は該当箇所を上書きし、過去案や廃止理由を追記しない。
- 比較案、検討メモ、承認履歴、検証ログはこの文書へ蓄積しない。
- 実装と文書がずれた場合は、ユーザー判断を確認して一方へ統一する。
- 新しいルート、コンポーネント、トークン、モーション、テーマを追加したときだけ更新する。
