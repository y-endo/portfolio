## デザインルール

デザインに関するルールは `DESIGN.md` を参照する。
なにかUIを編集・作成する場合は、必ず `DESIGN.md` のルールに従うこと。

## コーディング規約

- コンポーネントおよびページ固有のCSSはBEMに完全準拠し、Block、Element、Modifierのクラスだけでスタイルを指定する。
- コンポーネントおよびページ固有のSCSSでは、要素セレクター、IDセレクター、ユニバーサルセレクター、属性セレクター、位置依存の疑似クラスを使用しない。状態やバリエーションは `block--modifier` または `block__element--modifier` として明示する。
- HTMLの構造や要素の並び順に依存するセレクターは避け、スタイルが必要な要素には役割を表すBEMクラスを付与する。
- 長さの指定は共通のSass functionを必ず使用する。px相当値のrem換算には `rem()` を使用し、`px` と `rem` を直接記述しない。
- `rem()` に渡すpx相当値は整数にし、小数を使用しない。既存値を調整する場合は最も近い整数へ丸める。
- 小数は、透明度、単位なしの行間、比率、`fr`、`em`による字間、秒数、イージング、transform、シェーダー計算など、小数であること自体に意味がある値に限る。
- レスポンシブ指定は共通の `mq()` mixinへ定義済みのレイアウト名または能力名を渡し、`@media` を直接記述しない。
- `@include mq()` は、レスポンシブ指定の対象となるBEMセレクターの元のスタイル定義内へ必ずネストする。ファイル末尾やトップレベルにメディアクエリをまとめない。
- 同じ `mq()` 条件が複数のセレクターへ適用される場合も、各セレクターの定義内へそれぞれ記述する。基本スタイルとレスポンシブ差分を同じ場所で確認できるようにし、後から片方だけが変更される定義の分散を防ぐため。
- `:hover`、`:focus-visible`、`:open`などの状態を表す疑似クラスと、`::before`、`::after`、`::backdrop`などの疑似要素は、対象となる元セレクター内へ `&` で必ずネストする。元セレクターを繰り返したトップレベルの定義は禁止する。
- 同じ要素の複数状態は、元セレクター内で `&:hover, &:focus-visible` のようにまとめる。同一要素へ付与される外部ライブラリの状態クラスも `&.swiper-button-disabled` のように記述し、その状態に属する疑似クラスはさらに内側へネストする。
- 親Blockの状態によってElementを変更する場合は、Elementの元セレクター内に `.block--ready &` のように記述する。`--ready`、`--active`、`--loading`などの状態Modifierをトップレベルの子孫セレクターとして分離しない。
- 状態または疑似要素にだけ適用する `@include mq()` は、その `&` ブロック内へ記述する。適用対象を変えてしまうため、対応する単一の元セレクターを持たないグローバルな `::selection` は例外とする。
- 共通化できる値変換や定型宣言は、SCSSへ直書きせず既存のfunctionまたはmixinを使用する。適切なutilityがない場合は、用途と責務を確認したうえで `src/styles/tools/` に追加する。
- StylelintのBEM、単位、整数値、media queryの規則を無効化しない。例外が必要な場合は、理由と対象範囲をこのファイルおよびStylelint設定へ明記する。

## テスト方針

- このポートフォリオサイトは静的な表示を主体とするため、UI・文言・スタイル変更に対する自動テストの追加と実行は不要とする。
- 理由は、バックエンド処理や複雑な状態遷移を持たず、現段階では自動テストの保守コストが得られる効果を上回るため。
- 変更確認は `pnpm check`、または `pnpm lint` と `pnpm build`、および必要に応じたブラウザでの目視確認を基本とする。`pnpm check` はLintと静的ビルドのみを実行する。
- ユーザーから明示的に依頼された場合のみ、対象を限定してテストを追加または実行する。
